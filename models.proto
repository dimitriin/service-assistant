syntax = "proto3";

message CounterIncCmd {
    string name = 1;
    map<string, string> labels = 2;
}

message CounterAddCmd {
    string name = 1;
    map<string, string> labels = 2;
    uint64 value = 3;
}